# 1.2: Send Scheda Email

**Story ID:** 1.2
**Epic:** 1 — Scheda Management Workflows
**Slug:** send-scheda-email
**Status:** backlog

## Story

As an evaluator,
I want to email a completed Scheda to the employee with a PDF attachment,
so that they receive an official copy of their evaluation.

## Acceptance Criteria

<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. Evaluator can trigger "Send Email" from a Scheda action, visible only when status is `draft` or `submitted`, `sent_at` is null, and all scores are filled in.
2. System generates a PDF via Laravel Snappy (wkhtmltopdf) containing employee name, evaluation_type, year, per-criterion scores, evaluator name, evaluation date, and an evaluator signature placeholder field.
3. System queues an email to the employee's recorded email address with subject `"Your Performance Evaluation [{year}]"` and the generated PDF attached.
4. On successful hand-off to the mail transport, `Scheda.sent_at` is set to the current timestamp; it remains null until dispatch succeeds.
5. If the mail job fails (transport error), `sent_at` stays null, the Filament UI surfaces a flash error rather than an unhandled exception, and the failure is recorded for follow-up (full 3x-over-24h retry/bounce handling is delivered in Story 1.12).

## Tasks / Subtasks

- [ ] Add `sent_at` timestamp column migration on `schedas` table (AC: #4)
- [ ] Build `SchedaEvaluationMail` mailable + `emails/scheda-evaluation.blade.php` view (subject template, body summary) (AC: #2, #3)
- [ ] Build PDF Blade view `pdf/scheda.blade.php` (employee, scores table, evaluator, date, signature placeholder) rendered via Snappy (AC: #2)
- [ ] Create `SendSchedaEmailAction` that generates the PDF and dispatches the mailable (AC: #2, #3)
- [ ] Create queued `SendSchedaEmailJob` wrapping the action (AC: #3, #5)
- [ ] Add "Send Email" Filament action on `SchedaResource`, visible only when eligible per AC #1 (AC: #1)
- [ ] On job success set `Scheda.sent_at`; on failure leave it null and surface a flash error (AC: #4, #5)
- [ ] Feature test: dispatch with faked mailer/queue, assert PDF attached, subject correct, `sent_at` set only after successful processing (AC: #2, #3, #4)

## Dev Notes

<!-- LOCKED. External dev tools must not edit Dev Notes. -->

- [Source: prd.md#FR-002] "Email template: recipient (evaluator/staff), subject, body with Scheda summary"; "PDF: generated via Laravel Snappy (wkhtmltopdf), includes evaluator signature field (placeholder)"; "Delivery tracking: sent_at, bounced_at, opened_count"; "Retry logic: 3 attempts over 24h for hard failures"; "Success rate: ≥99.5% deliverable rate (NFR-P-002)."
- [Source: prd.md] (Epic 1 sample Story 1.2) subject `"Your Performance Evaluation [{year}]"`; "PDF includes: employee name, scores, evaluator name, date."
- [Source: architecture.md#adr-005] "Use Laravel Jobs for PDF, Email, Sigma Sync, and Archive Work" — request path stays short, failures are retryable; job payloads carry model IDs, not serialized blobs.
- [Source: architecture.md#component-design] the `Notification/PDF` component (`Notify` module) formally owns "Generate PDFs, send email, retry delivery, track status."
- [Source: epics.md#epic-1] Story 1.2 is assigned to the `Notify` module (`laravel/Modules/Notify/docs/stories/1.2.send-scheda-email.story.md`), matching architecture.md's component design where the Notification/PDF responsibility belongs to `Notify`, distinct from the `Rating` module that owns the `Scheda` model/resource (story 1.1). This story's mail/PDF/job classes live in `Modules/Notify`; the one shared file it modifies (`SchedaResource.php`) stays in `Modules/Rating` per that module's ownership.
- [Source: prd.md#NFR-P-002] delivery success target ≥99.5%; retry 3x over 24h — full retry/bounce mechanics are Story 1.12's scope, this story only records the single-attempt success/failure outcome.

## Testing

<!-- LOCKED. External dev tools must not edit Testing. -->

- **Unit:** PDF Blade view renders required fields given a `Scheda` fixture (string-presence assertions, not visual diff).
- **Feature:** `Mail::fake()` + `Queue::fake()` assert `SchedaEvaluationMail` queued with correct subject/recipient/attachment; process the job (e.g. `Bus::dispatchSync` or queue assertion) and assert `sent_at` set only after processing, never before.
- **Fixtures:** `SchedaFactory` state with all scores filled; a `withoutScores` factory state for the disabled-action test.
- **Edge cases:** send action hidden/disabled when scores incomplete; send action hidden when `sent_at` already set (this story sends once; resend is out of scope); simulated mail-transport exception leaves `sent_at` null and does not throw to the UI.

## Dependency Maps

- **Blocked by:** 1.1
- **Blocks:** 1.7, 1.12
- **External:** Laravel Mail transport configuration, Laravel Snappy + wkhtmltopdf binary installed in the environment, an active queue worker (queue availability tied to NFR-REL-001 uptime)

## Owned File/Module Scope

- `Modules/Notify/database/migrations/{ts}_add_sent_at_to_schedas_table.php` (new)
- `Modules/Notify/app/Mail/SchedaEvaluationMail.php` (new)
- `Modules/Notify/resources/views/emails/scheda-evaluation.blade.php` (new)
- `Modules/Notify/resources/views/pdf/scheda.blade.php` (new — **shared/contended**: read-only reuse by Story 1.7; that story must not restructure this file without updating this story's tests)
- `Modules/Notify/app/Actions/Scheda/SendSchedaEmailAction.php` (new)
- `Modules/Notify/app/Jobs/SendSchedaEmailJob.php` (new — **shared/contended**: extended by Story 1.12 for retry/backoff + delivery logging)
- `Modules/Rating/app/Filament/Resources/SchedaResource.php` (modify — add "Send Email" action only; shared base file owned by Rating module story 1.1, cross-module coordinate)
- `Modules/Notify/tests/Feature/Scheda/SendSchedaEmailTest.php` (new)

## Learnings from Previous Stories

(none — no prior completed Epic 1 stories at time of authoring)

## Dev Agent Record

_(empty — to be completed by the external dev tool)_
